server:
  port: 8081
  spring:
    cloud:
      config:
        server:
          bootstrap: true
          git:
            uri: https://github.com/CAArcos/tutorial-microservicios.git
            searchPaths: config-data
            username: {GIT_USER}
            password: {GIT_PASSWORD}
            default-label: "main"
    security:
      user:
        name: root
        password: s3cr3t
