server:
  port:8099

eureka:
  instance:
    hostname: localhost
  client:
    registerWithureka: false
    fetchRegistry: false
    serviceUrl:
      deafaultZone: defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
